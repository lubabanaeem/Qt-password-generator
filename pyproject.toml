[project]
name = "qt-desktop-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyside6>=6.11.0",
]

[tool.uv.workspace]
members = [
    "password_generater",
]
